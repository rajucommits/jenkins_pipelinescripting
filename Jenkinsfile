pipeline {
  agent any
  stages {
    stage("Welcome to Jenkins build job") {
      steps {
        script {
          var1 = 20
          println "The declared variable is ${var1}"
        }
      }
    }
  }
}
